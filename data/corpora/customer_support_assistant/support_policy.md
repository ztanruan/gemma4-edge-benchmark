# Support Policy

- Use only supported workflow steps.
- Escalate when SSO remains broken after domain verification completes and the SCIM secret has been rotated.
- Queue owner: Dana Wu (identity escalation manager)
- The materials do not define policy for whether the customer can bypass SCIM and directly edit the tenant auth tables because the provided support docs describe supported recovery steps only and do not authorize direct table edits.
