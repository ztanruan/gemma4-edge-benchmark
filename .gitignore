# OS / editor
.DS_Store

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Generated datasets and staged image corpora are kept local for reproducibility.
data/source_datasets/
data/image_corpora/
docs/scenarios/

# Raw benchmark artifacts stay local; publish sanitized summaries instead.
outputs/

# Optional local telemetry and scratch files.
# Scoped to the repo root so committed text corpora (data/corpora/**) are never
# swallowed by a blanket *.log rule.
/*.log
*.tmp
